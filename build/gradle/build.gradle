task hello {
    doLast {
        println();
        println("******************");
        println("Welcom to Gradle");
        println("******************");
        println();
    }
}

task parameter {
	doLast {
		def total = 0;
		for (def i in 1..num.toInteger()) {
			total += i;
		}
		println("total :" + total);
	}
}